# CDN script index

The canonical scripts remain in `scripts/` so existing commands and relative paths do not break.
Each subfolder contains the scripts intended for that CDN and points to the canonical file.
